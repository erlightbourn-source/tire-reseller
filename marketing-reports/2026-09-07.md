# TireTrader — Growth & UX Funnel Review
**Advisory memo · read-only analysis · 2026-09-07**

Scope: a status re-check of 8/24's four carried findings (T2/T3/T4/A1, tracked in unmerged PR #23
and re-confirmed stale in the 8/31 review) against `main` as of `803de91` (the 2026-09-06 one-plan/
two-price rewrite touched most of the pages these findings live on). Every line cited below was
re-read on the live file this pass, not carried over on trust. No new full sweep this week — the
pricing rewrite is the dominant event since 8/31, so this review is scoped to it.

---

## STATUS CHECK — 8/24 findings (T2/T3/T4/A1) vs. current `main`

- **T2 — pricing copy conflict: RESOLVED.** Confirmed by direct read: `app/founding-seller/page.js`,
  `app/sell-tires/page.js`, and `app/subscribe/page.js` no longer hardcode `$10`/`$25` anywhere —
  every price string now derives from `lib/pricing.js` (`FOUNDING_LABEL`/`STANDARD_LABEL`/
  `PLAN_COPY.foundingStory`, "the first 25 Founding Sellers lock in $10/month for life; every seller
  after pays $25/month"). One story, one source of truth, landed as part of 803de91.
- **N1 — `/pro`'s "simulated" disclaimer not tied to real Stripe state: RESOLVED.** `app/pro/page.js`
  now imports `stripeConfigured()` (line 5) and branches the checkout-note string on it (line 92),
  mirroring `/subscribe`'s existing pattern (line 100) exactly — the two pages no longer disagree.
- **A1 — no Founding Seller mention on `/sell-tires`: RESOLVED.** `PLAN_COPY.foundingStory` now
  appears three times on the page (hero CTA row line 58, pricing-card line 115, meta description
  line 13) — the founding-seller incentive is now visible before a prospect even scrolls.
- **STILL OPEN — T3, unchanged, still a business decision, not a code bug.** `app/page.js:29`,
  `lib/content.js:52`, `app/founding-seller/page.js:25` still call the seller-plan badge "verified"/
  "trusted"; `lib/seller.js`'s `isProSeller()` is still a pure billing flag (`!!(s.pro ||
  s.foundingSeller)`) — paying is the only "verification" happening. Decision point for Evan: soften
  the copy to match reality, or build an actual verification step (ID, business license, etc.).
- **STILL OPEN — T4, unchanged.** `app/listings/[id]/page.js:237` still hardcodes "⚡ Usually
  responds within a day" for every paid/founding seller — not computed from `Message`/`Thread`
  timestamps anywhere in the codebase. Low risk to leave as aspirational copy pre-launch (no seller
  has a real reply-time history yet), but it becomes a real trust liability once sellers with slow
  reply times exist. Not marked data-engineer-safe last review because computing it needs a
  read-query design decision (rolling window? per-seller average? cache it or compute live?) —
  still true.

**Net for this pass: 3 of 4 carried findings are now resolved — not by an advisory-driven fix, but as
a side effect of yesterday's pricing rewrite reaching the same pages.** That is not what a copy fix
usually looks like and shouldn't be read as a pattern (i.e., don't wait on the next big refactor to
close open UX findings) — but it does mean the backlog realistically shrinks to just T3 (business
decision) + T4 (needs a query design, not a copy edit).

---

## TOP NEXT

1. **T3 — decide the "verified" language.** S-effort either way (soften copy) or M/L (build real
   verification). Evan's call — flag, don't implement either option unprompted.
2. **T4 — design the real "responds within X" metric** (needs-plan: a bounded seller-level query
   over `Message.createdAt` per thread, cached or computed at read time). Not urgent pre-launch
   (~0 real sellers today per 8/24's context note) but worth a plan before real sellers exist so it
   isn't shipped as a hardcoded lie.
3. Nothing else rises above these two — the rest of the 8/24 sweep is closed. Recommend Evan/a
   reviewer merge or close PR #23 (superseded — see this run's safety report, stale-PR section).

No monetization, schema, or design-system changes proposed. No code touched by this review.
