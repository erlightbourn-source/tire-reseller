# TireTrader Safety + Bug Audit — 2026-07-01 16:15 EDT (re-verification run)

**Branch:** `de/safety-20260701` (off latest `main`; `main` never touched)
**Auditor:** `data-engineer` subagent, independently reviewed
**Context:** This is the **second invocation today**. A comprehensive pass ran at 08:10 (report `2026-07-01-0810.md`) on this exact code. Since then the repo has had **zero commits** — `origin/main` unchanged at `cea2b06`, this branch still `cdce203` (2 commits ahead: fix `13494fc` + morning report), working tree clean. This run re-verifies the green state and does a genuine fresh scan; it does not re-do the morning fix.

---

## (a) Fixed — committed on `de/safety-20260701`

**Nothing new committed this run.** The code surface is byte-identical to the 08:10 pass, whose one fix is already on this branch and in open PR #1:

- **`13494fc`** — Apply the soft-deleted-seller visibility filter (`seller: { deletedAt: null }`) to all public listing feeds: browse, homepage feeds/stats, size/brand landing pages, listing "more like this", sell-tires active counter, sitemap. Data-integrity / defense-in-depth, behavior-preserving for legitimate sellers. (Full detail in `2026-07-01-0810.md`.)

### Re-verification (independent, this run) — all GREEN

| Check | Command | Result |
|---|---|---|
| Build | `APP_SECRET=… npm run build` | Succeeded — full route table compiled, middleware 34.4 kB |
| Tests | `npm test` | **61/61 pass**, 0 fail |
| Prod audit | `npm audit --omit=dev` | **0 vulnerabilities** |
| Schema | `sed -n '9p' prisma/schema.prisma` | build flipped to sqlite → restored to `provider = "postgresql"` |
| Tree | `git status --short` | clean; HEAD `cdce203`, origin/main `cea2b06` |

The morning pass's PASS/A grade reproduces independently.

---

## (b) Flagged for review

No new findings. One low-severity item **carried over** from the morning pass, still open (auth-flow category → do NOT auto-fix per hard rules):

1. **[Low] Per-recipient email cap missing on password-reset and resend-verification** — `app/api/auth/forgot/route.js:10` and `app/api/auth/resend-verification/route.js:12` rate-limit per-IP only (5/min). An IP-rotating attacker could bomb one victim's inbox with reset/verify emails. `app/api/email-alerts/route.js:24` already closes this with a per-address bucket.
   - **Recommended fix (mirrors existing pattern):** after the per-IP `enforceRateLimit`, add a per-address cap, e.g. `enforceRateLimit(req, "forgot-addr", { key: addr, limit: 3, windowMs: 60*60*1000 })`.
   - **Why human:** touches the auth flow (hard-rule flag-only category); a human should confirm the cap won't lock out legitimate retry patterns.
2. **[Low] Plaintext `email-alerts` confirm token** — noted in the morning report; benign opt-in action, left as-is for consistency only.

Fresh trace this run confirmed the rest of the surface remains sound: ownership on every `[id]`/`[threadId]` route (`requireOwner`, `loadThread`, page redirects, `listings/[id]:64` notFound), atomic single-use claims (guarded `updateMany` on reset/offer/sale/report), denorm sync (`sellerPro`, `ratingAvg/ratingCount`, `deriveListingColumns`), CSRF/CSP middleware, per-IP + per-recipient rate-limit support, upload hardening (magic-byte + 8 MB cap + Content-Length reject + EXIF strip), and escaped-only `dangerouslySetInnerHTML` (JSON-LD via `lib/jsonld.js` / static SVG maps).

---

## (c) Review Verdict

**VERDICT: PASS**
**GRADE: A**

Independent re-read of this run's state and the subagent's trace:

- **Green state reproduced independently.** Build succeeded, `npm test` 61/61, `npm audit --omit=dev` 0 vulns, schema restored to `postgresql`, tree clean. Nothing was committed this run, so there is no new diff to over-claim on.
- **Fresh scan was genuine, not a rubber-stamp** — 48 tool calls tracing every mutating route and dynamic page for authorization/ownership, atomicity, and denorm sync; it surfaced no new issue and correctly re-identified the one open auth-email gap as flag-only.
- **Correct dedup decision:** the branch already carries a real fix + open PR #1, so it was NOT deleted despite "nothing new actionable" — deleting would destroy the morning's legitimate work. This report is added to the same branch and PR #1 is updated rather than opening a duplicate.
- **Nothing auto-fixed that should have been flagged** — nothing was auto-fixed at all; the only candidate (per-recipient email cap) stayed correctly in the human-review lane.

No fixes reverted. No REVISE actions.
