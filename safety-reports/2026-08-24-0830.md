# TireTrader safety + improvement report — 2026-08-24

**Audited commit (main HEAD):** `55b5d1e` — unchanged since the 2026-07-06 report merged to
`main`; every safety pass since (7/9 → 8/20) has landed on an open PR that never merged (see
(g)). This run continues **PR #22** (`de/safety-20260820`) instead of opening a fourth
divergent branch — see "Branch note" below.

## Branch note (process finding, not a code finding)
`main` has not merged a `data-engineer` safety PR since **2026-07-06** — PRs #13, #15, #16
were closed without merging; #21 (8/17) and #22 (8/20) are still open. Continuing to fork a
new `de/safety-$(date)` branch off stale `main` every run — as the routine literally
specifies — would have created a **third** unmerged branch this week, each diverging further
and each needing its own review. Instead, today's work continues directly on **PR #22**
(branch `de/safety-20260820`), and folds in the one piece of real, already-verified value
sitting on **PR #21** (the CVE patch-bump, see (c)) so #21 can be closed as redundant rather
than left to rot. **This is a one-time consolidation, not a new pattern** — once Evan/a
reviewer merges #22, the routine reverts to branching off `main` each run as designed. Flagging
because 5 unmerged safety/report PRs (soon 3 once #21 is closeable) sitting for weeks means
findings from PAST runs are not actually protecting the live site.

## (a) Fixed
1. **`5322755`** — `components/AuthForm.js` unconditionally showed the real, working demo
   login credentials (`demo@tiretrader.test` / `demo1234`, a live seeded account,
   `prisma/seed.js:116`) to every anonymous `/login` visitor on the deployed production site.
   Anyone could sign into that account. Gated on `NODE_ENV !== "production"` (same pattern
   `lib/auth.js` already uses); still visible in local dev. Sourced from today's marketing
   review (see below), which correctly called it the top trust/security finding. Verified
   live: curl of a `next start` (production) build shows **0** occurrences of the credential
   string in the served `/login` HTML (present before the fix).

## (b) Improved
1. **`941620f`** — BACKLOG #6 (accessibility). `MarketplaceFilters.js`'s `Field` wrapper
   rendered its visible label as a `<p>`, not a `<label htmlFor>`, so 9 controls on the
   primary browse/filter surface (radius, sort, brand, size, max price, min tread, min DOT
   year, quantity, seller rating, season) had **no accessible name** — a screen reader
   announced a bare "combobox"/"textbox". Added `aria-label` to each, matching the pattern
   `HeroSearch.js` already used correctly for its Make/Model/Trim/Year selects. Pure additive
   attribute, zero behavior change. Verified live: `next start` + curl of `/browse` shows all
   9 `aria-label` attributes rendered in the served HTML.
2. **BACKLOG.md refreshed 3x** across the run (`e9a1441`, plus the two below) — #6 moved to
   in-progress (listing-detail focus/contrast still open), #15 marked landed-to-branch, #16
   and #17 added.

## (c) Proposed for review
1. **`ee7afc6`** — PROPOSAL: `next` 15.5.19→15.5.23 + `postcss` 8.5.15→8.5.26, closing 4
   HIGH-severity `npm audit` CVEs (Server Action DoS/SSRF, cache-confusion, SVG DoS,
   unauthenticated Server Function disclosure; postcss sourcemap path traversal). **This is
   not new work** — it's Evan's own commit `63ef440`, cherry-picked from PR #21 (open 7 days,
   unreviewed) onto this branch so the fix actually lands somewhere current instead of being
   flagged a fourth time. Re-verified here: build clean, 66/66 tests pass,
   `npm audit --omit=dev --audit-level=high` now shows only the pre-existing `sharp` item
   (below), schema restored to postgresql.
2. **`468c96f`** — PROPOSAL: drop an enumeration hint from `/api/email-alerts` — an existing
   `(email, query)` pair returned `{ok:true, duplicate:true}`, distinguishable from the
   fresh-create `{ok:true}` response, letting a caller learn whether a specific email is
   already registered for a search. Now neutral, matching the enumeration-safe symmetry
   BACKLOG #1 already established for forgot/resend-verify. Also cherry-picked from PR #21
   (Evan's commit `2974f0a`) for the same reason as above. Verified: build clean, 66/66 tests
   pass. Not covered by an automated regression test (BACKLOG #3b — no DB-backed test lane
   exists yet); flagging that gap rather than claiming coverage that isn't there.

## (d) Flagged (not implemented)
- **`sharp` 0.34.5→0.35.3** — closes libvips CVEs (CVE-2026-33327/33328/35590/35591, high) but
  is a MINOR version bump — flag-only per the dependency hard rule, carried unchanged since
  2026-08-17.
- Security-floor spot check: no application code changed on `main` since the 2026-08-17 full
  audit except a single CSS class on `HeroSearch.js` (`55b5d1e`, Evan's own visual fix, not a
  code-path change) — re-checked `dangerouslySetInnerHTML` usage (all either the escaping
  `jsonLdHtml()` helper or static icon-map constants, no user input) and `[id]`-route
  ownership: nothing new. Abbreviated re-verification, not a full re-tread, per the
  unchanged-code fast path — the freed effort went into (a)/(b)/(c)/(e2) instead.
- BACKLOG #2.5 (launch-readiness e2e sweep), #3a/#3b (needs-plan token-hardening + test
  infra), #4 (Playwright funnel expansion) remain pending, deprioritized this run.

## (e) Launch-blocker count
**0** code-side blockers (unchanged). Remaining GO-LIVE.md items are provisioning/ops calls
owned by Evan. Separately — **not a code blocker, but a live operational risk**: the Vercel
Pro trial deadline (Sun 8/23 20:55 EDT) passed with no decision on file; site is still up but
SUSPECTED (not confirmed) reverted to the non-commercial-ToS Hobby tier. Already flagged
directly to Evan this morning (06:2x ET) and by Miles (3 touches since 8/21) — not
re-escalating here to avoid a duplicate ping, see `memory/tiretrader-sofla-launch.md`.

## (e2) Weekly marketing/UX review (Monday)
Ran the lapsed weekly review (last one was 6 weeks ago, 2026-07-06) via a general-purpose
subagent standing in for the `marketing` agent type. Report: `marketing-reports/2026-08-24.md`
on branch `mktg/review-20260824`, opened as **PR #23** (report-only, no code touched — verified
by the PR's file list). Top finding (the exposed demo credentials) was actionable and small
enough to fix same-run — see (a). Other top findings, left for Evan/product: `/sell-tires`
never mentions the Founding Seller program despite every "Start selling" CTA landing there;
"Verified Pro sellers" copy appears on 4 surfaces but no verification step actually exists in
`lib/seller.js`; `/pro`'s "Demo: upgrade is simulated" text is unconditional unlike
`/subscribe`'s correctly-gated version; and the founding-seller page's "$25/mo after the first
25" framing isn't repeated on `/sell-tires`/`/subscribe`, which just say "$10/month, cancel
anytime" — a buyer-facing pricing-clarity gap, not a monetization change.

## (f) Review verdict
**PASS.** Independent re-read of this run's diff: every commit is either (1) a pure additive
attribute (`aria-label`), (2) a narrow env-gate on a static string, (3) two already-verified
cherry-picks of Evan's own prior commits, or (4) docs/backlog. All five build+test gates were
re-run and passed cleanly (not just claimed from the cherry-pick source). Two fixes ((a) and
(b)) were also verified by a live `next start` + curl smoke test, not build/test alone —
correctly following the 2026-08-20 lesson that App Router status/render behavior needs a real
request, not just a green build. Nothing here touches schema, monetization, or an
auth-flow-behavior change beyond the two already-reviewed-and-authored PROPOSALs. Grade: **B+**
— real, verified fixes shipped (including a genuine security exposure caught by the marketing
review and closed same-run), two week-old stalled proposals finally landed somewhere current,
and the branch-fragmentation problem was surfaced with a concrete fix rather than silently
tolerated. Held back from an A by the fact that this branch-consolidation move is itself a
deviation from the written routine — flagged transparently above, not hidden, but it's a
judgment call a human should sanity-check.

## (g) Stale PRs
- **#21** `de/safety-20260817` (7d, now stale) — its only unique value (the CVE bump `63ef440`
  and the enumeration-hint fix `2974f0a`) is now duplicated onto **PR #22** with fresh
  verification. **Recommend: close #21** once #22 is reviewed/merged — nothing left worth a
  separate merge (its other 3 commits are a loading.js add+revert wash and its own now-superseded
  report file).
- **#18** `de/cf-port-spike-20260719` (28d) — Cloudflare/OpenNext spike, unchanged bit-rot
  status from 8/20's check (`prisma/schema.prisma` still unchanged since its merge-base). Large
  (6.8k-line, mostly lockfile) diff awaiting Evan's infra-path decision. No new action.
- **#14** `mktg/review-20260713` (42d) — report-only, superseded in substance by today's #23
  (which explicitly re-verified and credited what #14's era of findings already fixed). Low
  risk either way; **recommend closing #14** now that #23 exists as the current source of truth,
  to stop the marketing-report PR count growing unbounded.
- **#12** `de/liability-terms-20260709` (46d) — legal terms, HOLD for counsel per its own title.
  Re-checked via `git merge-tree`: still applies cleanly against current `main`. No engineering
  action; awaiting Evan/counsel.
- **#22** `de/safety-20260820` (this run's branch, 4d) — now carries 5/6 weeks of accumulated,
  re-verified safety work. **Recommend this is the one to prioritize reviewing/merging.**
- **#23** `mktg/review-20260824` (today, not stale) — new, report-only.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
