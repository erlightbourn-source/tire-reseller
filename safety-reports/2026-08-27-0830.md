# TireTrader safety + improvement report — 2026-08-27

**Audited commit (main HEAD):** `55b5d1e` — unchanged since the 2026-07-06 report merged to
`main`. This run continues on **PR #22** (`de/safety-20260820`) rather than forking a fifth
branch — see "Branch note" below, which is now a bigger deal than last time.

## Branch note (process finding — escalated this run)
`main` has not merged a `data-engineer` safety PR since **2026-07-06**, and PR #22 is now
**7 days old** (opened 2026-08-20, previously updated 2026-08-24, now again today). The
2026-08-24 report already flagged this as a "one-time consolidation, not a new pattern" —
that didn't hold; #22 is still open a week later. **Concretely verified this run:** the
credential-leak fix committed 2026-08-24 (`5322755`, gating the demo login credentials
banner to non-production) is sitting on this unmerged branch, and `curl
https://shoptiretrader.com/login` **still serves `demo@tiretrader.test` / a live,
signable-into account to every anonymous visitor right now** — the fix has been written,
verified, and undeployed for 3 days. This is a live security exposure, not a theoretical
one. Recommending in today's Telegram alert that Evan (or a reviewer) actually merge #22
today. I did not merge it myself — that's a deploy-affecting decision outside this routine's
authority, and the branch also carries an as-yet-unreviewed decision point (BACKLOG #2, the
soft-delete restore threshold, still `proposal`).

## (a) Fixed
None this run (abbreviated fast-path audit — see below).

## (b) Improved
1. **`83c09ff`** — BACKLOG #4 (test-coverage), **SHIPPED**. New e2e test covering the full
   core funnel: seller signs up → verified → lists a tire → buyer opens a thread → sends an
   offer → seller accepts → listing flips to `sold`. Also asserts a non-participant is denied
   thread access (403) and that re-accepting an already-answered offer 409s (the atomic
   accept-race guard actually holds under test, not just by inspection). Verify-token bypass
   uses a direct Prisma write mirroring what `/api/auth/verify` does — there's no dev email
   outbox yet (BACKLOG #3b), so this is the smallest correct way to get a verified test user
   without touching production code. Measurable effect: e2e coverage 9 → 10 tests; this is
   the first automated coverage of the listing/thread/offer path (previously only auth flows
   were covered). Verified: `npm run build` clean, `npm test` 66/66, `npm run test:e2e` 10/10
   (all green on first run), schema restored to postgresql.
2. **`3807fbc`** — BACKLOG #6 (accessibility), **CLOSED, no code change**. Audited
   listing-detail focus-states and contrast (the item's remaining open scope after the
   2026-08-24 browse-filter fix). Every interactive element on the page and its shared
   components is a real `<a>`/`<button>`, already covered by the global focus-visible ring in
   `app/globals.css`; no `<div onClick>` anti-pattern exists to fix. Contrast checked
   quantitatively (WCAG relative-luminance calc, not eyeballed) for the page's lowest-contrast
   text: `text-slate-400`/black 8.2:1, the "before you buy" amber safety box 11.4:1/16.1:1 —
   all well above the 4.5:1 AA floor. Closing with evidence beats leaving it open on a guess.

## (c) Proposed for review
None net-new this run (existing proposals `ee7afc6` next/postcss CVE bump and `468c96f`
email-alerts enumeration fix remain on this branch from 2026-08-24, unmerged, both
re-verified clean below).

## (d) Flagged (not implemented)
- **`sharp` 0.34.5→0.35.3** — closes libvips CVEs (CVE-2026-33327/33328/35590/35591, high),
  minor version bump → flag-only per the dependency hard rule. Carried unchanged since
  2026-08-17; re-confirmed via `npm audit --omit=dev --audit-level=high` today, no new
  advisories surfaced (still exactly the sharp + next-depends-on-sharp pair, `next audit fix
  --force` would pull an out-of-range next — still not auto-fixable safely).
- Security-floor spot check: `main` HEAD unchanged (`55b5d1e`) since the last full audit —
  abbreviated re-verification per the unchanged-code fast path, no new code paths to check.
  The freed effort went into (b) above instead, one item deeper than a normal fast-path run.
- **Live operational risk, re-flagged:** the credential-leak fix and the CVE/enumeration
  proposals are all correct-and-verified but not live — see Branch note above. Not a code
  finding; a merge-authority gap.
- BACKLOG #2.5 (launch-readiness e2e sweep), #3a/#3b (needs-plan token-hardening + test
  infra), #7 (performance, no load data to act on) remain pending, deprioritized this run.

## (e) Launch-blocker count
**0** code-side blockers (unchanged). The live operational risk above (unmerged security fix)
is not a "launch" blocker — TireTrader is already live — but it is an active production
exposure and is being escalated as such, separately from the launch-readiness count.

## (f) STALE PRs
`gh pr list --state open`:
- **#22** `de/safety-20260820` — "data-engineer: safety + improvements" — open **7 days**
  (2026-08-20 → today), now updated again by this run. Contains real, verified, unmerged
  fixes including a live credential-leak patch. **Recommend: merge today**, not close — the
  content is current (re-verified against unchanged `main` HEAD both 2026-08-24 and today).
- **#18** `de/cf-port-spike-20260719` — "SPIKE: Cloudflare/OpenNext port + R2 uploads" — open
  **39 days** (2026-07-20). Title says "unpushed, needs a path decision" — this is an
  intentional hold on a product/infra decision, not neglect. No bit-rot risk assessed (it's a
  spike, not a landed-fix branch); recommend Evan close or convert to a decision ask, not a
  code action from this routine.
- **#12** `de/liability-terms-20260709` — "legal: liability terms..." — open **49 days**
  (2026-07-09), explicitly titled "HOLD for counsel." Deliberate, not stale in the
  neglect sense — no action recommended.
- **#23** `mktg/review-20260824` — Monday marketing report, report-only, no code — not part
  of this section's remit (routine PR, not a safety/fix branch).

## (g) Review verdict
**VERDICT: PASS.** Every committed change (`83c09ff`, `3807fbc`) passed the full build+test
gate (66/66 unit, 10/10 e2e including the new test) before commit — shown above, not
asserted. Nothing was auto-fixed that should have been a proposal or flag instead (both
commits are test/docs-only, zero app-behavior change). #4's claimed effect (e2e 9→10, real
funnel coverage) is directly visible in the `e2e/flows.mjs` diff and the test run output.
#6's "closed, no gap" claim is backed by the quantitative contrast numbers and the
component-by-component focus-target grep, not an assumption. No REVISE items.

**Residual honesty note:** this was a fast-path run by design (main HEAD unchanged) — the
main news is the branch-pileup escalation in the Branch note, not new code findings.

**Verification summary:** `npm run build` clean · `npm test` 66/66 · `npm run test:e2e` 10/10
· `npm audit --omit=dev --audit-level=high` unchanged (1 carried flag) · schema provider =
postgresql · working tree clean · `main` not touched.
