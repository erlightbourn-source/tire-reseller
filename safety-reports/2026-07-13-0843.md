# TireTrader Safety + Improvement Audit — 2026-07-13 08:43 ET

**Branch:** `de/safety-20260713` (off `main`; `main` never pushed)
**Audited commit (main HEAD):** `c688880` ← record for the unchanged-code fast path
**Auditor:** Dev, full audit (main changed since the last audit: `7e4745b` → `c688880`, merging the brutalism-restore, hygiene-contrast, node-runtime, and liability-terms branches). Independent staff-engineer + app-sec reviewer spawned on the current codebase (two-expert gate); Monday, so the marketing/UX review also ran (separate PR #14).

---

## ⚠️ (0) Headline finding — orphaned Founding-Seller WIP recovered from `main`'s working tree

At run start, `main` had **uncommitted working-tree changes** (11 modified files + a new `lib/seller.js`, and `prisma/schema.prisma` line 9 flipped to `sqlite`). This was a coherent, **incomplete "Founding Seller" feature** left uncommitted by a prior interrupted run — it adds a `foundingSeller` column to the `User` model, a write-time `sellerPro` mirror, a `FoundingBadge`, and an `isProSeller()` helper. It matches the approved SoFla-launch promo (memory `tiretrader-sofla-launch.md`), but:

- It **touches `prisma/schema.prisma` (a NEW column)** and has **no generated migration**, so it would not build as-is. Schema/migration work is **flag-only** per the hard rules — it must never be auto-committed.
- It was sitting dirty on `main`, which violates the clean-tree invariant and risked being lost or clobbered by any future `git` operation.

**Action taken (damage control, not endorsement):** preserved the entire WIP on a dedicated local branch **`de/founding-seller-wip-20260713`** (one commit, schema restored to `postgresql` for coherence) so the work is recoverable, then restored `main` to a clean tree. **This is a FLAG for Evan** — the feature needs your product decision + a proper Prisma migration before it can ship. To review it: `git checkout de/founding-seller-wip-20260713`. Nothing about it was pushed.

---

## (a) Fixed — committed on `de/safety-20260713`

**No auto-fix bug fixes this run.** The independent security audit (below) found the floor clean with no new issues or regressions; `npm audit` = **0 vulnerabilities**.

## (b) Improved — shipped this run

**`73067f8` (BACKLOG #5) — Add a loading skeleton to the inbox.**
`app/messages/page.js` ran **two heavy sequential DB queries** (findMany threads with nested includes, then a groupBy for unread counts) with **no loading boundary** — a slow request showed a blank screen. Added `app/messages/loading.js`, a skeleton matching the inbox row layout.
- **Measurable effect:** the auth-gated inbox now streams an instant skeleton instead of a blank wait; routes with a loading boundary **6 → 7**.
- **Deliberately scoped (and documented in BACKLOG #5 so a future run doesn't regress it):** the async detail/`[id]` + SEO routes (`listings/[id]`, `sellers/[id]`, `sizes/[size]`, `tires/[brand]`, `messages/[threadId]`) all call `notFound()` and must **NOT** get a `loading.js` — a boundary flushes a 200 shell that turns real 404s into **soft-404s** (see `components/PageLoading.js`). `messages` (list) `redirect()`s instead, so it is the one safe gap.
- **Verified:** `APP_SECRET=… npm run build` OK; `npm test` **61/61**; `/messages` route compiled; schema line 9 = `provider = "postgresql"`.

Backlog refreshed: #5 → shipped; Monday marketing findings added as #15–#19 (see below).

## (c) Proposed for review — committed, needs Evan's PR review

**None new this run.** (The prior confirm-token-hash PROPOSAL rides in the still-open PR #13.)

## (d) Flagged for review (description-only — need a human/product decision)

1. **[HIGH — product decision] The recovered Founding-Seller WIP** — see §(0). Schema change + missing migration; preserved on `de/founding-seller-wip-20260713`. Needs your go/no-go + a proper migration.
2. **[Med — launch-readiness, BACKLOG #15] Dev/demo UI leaks to production.** The login form renders `Demo: demo@tiretrader.test / demo1234` unconditionally on the login tab (`AuthForm.js:205-207`); `/pro` shows "⚙️ Demo: upgrade is simulated" (`app/pro/page.js:57`). Confirmed this run. **Not auto-changed on purpose:** gating on `NODE_ENV` would strip demo access from your current live preview mid-demo-phase. The right fix is an explicit `NEXT_PUBLIC_DEMO_MODE` flag kept ON until launch — a small behavior change, so a PROPOSAL when you want it, not a silent edit now.
3. **[Med — ops, carried, BACKLOG #3c] Local dev Node < patched line.** Node HIGH CVEs are fixed in ≥22.22.0; `.nvmrc` + `engines` are pinned to 22.22.0 (Vercel reads it), but local dev Node was 22.14.0. `npm audit` = 0 (these are runtime, not dependency, CVEs). Confirm the Vercel/deploy Node is ≥22.22.0. **This run's build/test ran on Node 22.22.0.**
4. **[Low — carried, BACKLOG #3a] Unsubscribe token plaintext at rest.** A persistent capability the alerts cron re-emits per digest, so it can't be hashed at rest — needs a per-email HMAC redesign (needs-plan). 256-bit unguessable, unsubscribe-only.
5. **[Low — trust/copy, BACKLOG #17] "Verified Pro" overstates what's verified.** Nothing is actually verified (only email + paid tier). Copy softening is safe; the real verification signal tied to the Founding Seller promo = your product decision.

## All-clear areas (independently re-verified this run, with evidence)

The spawned staff-engineer/app-sec reviewer verified the floor holds on live code and found **no new issues or regressions**:
- **AuthZ / no IDOR — every id-scoped route enforces ownership:** `api/listings/[id]` (`requireOwner`: 401→404→403; refuses `featured` from owner input), `api/messages/[threadId]` (buyer/seller-only, re-checks blocks per send), `api/offers` (thread-membership + no-self-answer + pending-guard + atomic `updateMany` — no double-accept race), `sell/[id]/edit` (owner redirect + scoped select), `sellers/[id]` (only `id` + scalars reach client components), `api/admin` (`isAdmin` + rate limit + audit log).
- **Auth/session (`lib/auth.js`):** JWT alg pinned HS256 (sign + verify); `APP_SECRET ≥32` fail-closed on prod/preview; cookie httpOnly+sameSite=lax+secure(prod); `tokenVersion` revocation; bcrypt-12 + SHA-256 prehash (no 72-byte truncation); reset/confirm tokens SHA-256 hashed at rest.
- **XSS:** all 9 `dangerouslySetInnerHTML` sites are JSON-LD (escaped incl. U+2028/2029) or static icon maps — no user data reaches raw HTML.
- **SQL:** no `$queryRaw`/`*Unsafe` — Prisma only.
- **Rate limiting:** present on every auth/email/upload/mutation route (IP- and address-scoped).
- **Upload (`api/upload`):** auth-gated, rate-limited, content-length pre-check, magic-byte sniff, 8MB×6 caps, Exif/GPS stripped, server-generated filenames (no traversal).
- **Cron/webhook:** `cron/alerts` + `cron/purge` bearer-gated on `CRON_SECRET`, fail-closed, `timingSafeEqual`; Stripe webhook verifies signature via `constructEvent` on the raw body.
- **Data integrity:** `listings/[id]` PATCH keeps denormalized size/tread/per-tire columns in sync via `deriveListingColumns()`.
- `npm audit` = **0**. Next 15.5.19 (≥ patched 15.5.18). Node pinned 22.22.0.

**Launch blockers remaining (code-side): 0.** (The Founding-Seller WIP is a *new feature* awaiting your decision, not a blocker on the current build.) Remaining GO-LIVE items are provisioning/ops owned by Evan; Stripe live-mode + deploy are Evan's calls.

---

## (e) Review Verdict

**VERDICT: PASS**
**GRADE: A−**

Independent re-read of this run's committed diff + the flagged list, plus the spawned staff-engineer/app-sec reviewer (two-expert gate):

- **The one shipped change (`73067f8`, loading skeleton) is genuinely additive and correctly scoped.** Its claimed effect is real in the diff (new `app/messages/loading.js`; boundary count 6→7). Build passed, 61/61 tests, schema line 9 = `provider = "postgresql"`, tree clean. The soft-404 reasoning was verified against the actual `notFound()` callsites in each candidate route — the omission on detail/`[id]` routes is correct, not a gap.
- **The recovered Founding-Seller WIP was handled correctly:** preserved (not lost), not committed to `main`, not shipped as a proposal (schema/migration = flag-only). Surfaced as the headline flag with recovery instructions. `main` ends clean.
- **Nothing auto-fixed that should have been flagged.** Demo-UI gating and the "Verified" copy were deliberately *flagged*, not silently changed, because both would alter behavior Evan currently relies on (live demo access; brand copy).
- **Reconciliation:** the reviewer's clean pass and my read agree; no unresolved disagreement. **AGREED.**
- **Honest residual:** the loading skeleton is verified by build (route compiles) + the notFound() analysis, not by a throttled headless render — the same practical residual class prior UI-boundary runs accepted. No wired-in test locks a Suspense fallback.

No fixes reverted. No REVISE actions.

## (f) Stale PRs (>7 days)

**None.** Open PRs at report time, all within the 7-day window: **#14** `mktg/review-20260713` (today, this run), **#13** `de/safety-20260709` (4d, carries the confirm-token proposal), **#12** `de/liability-terms-20260709` (4d, HOLD for counsel). #12 and #13 are approaching the window — worth a review pass this week.
