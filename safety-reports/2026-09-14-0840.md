# TireTrader Safety + Improvement Report — 2026-09-14

**Audited commit (main HEAD):** `3a803c0` (branch `de/safety-20260914` cut from it).

Not a fast-path run: HEAD has moved substantially since the last full audit whose report
actually merged (2026-09-07 report, audited commit `37ed302`) — main has since absorbed the
messaging-notification + signup-consent-gate build (`0e23058`, PR #34) and the admin-grant
script (`3a803c0`, PR #35). Ran the full audit.

## (a) Fixed
- **`e2e/flows.mjs` — signup consent-gate regression (`7be6055`).** The 2026-09-13 signup Terms
  consent gate (`0e23058`, PR #34) added a server-side `agreedToTerms===true` requirement to
  `POST /api/auth/signup`, but PR #34's own verification only ran the unit suite (`npm test`),
  not the separate e2e lane (`node --test e2e/flows.mjs`). Result: 2 of 11 e2e tests have been
  silently red on `main` since the merge — both signup call sites omitted the flag, so every
  signup call in the harness got a 400 instead of the expected 200. Fixed both call sites, and
  added a dedicated negative-path test so the consent gate itself has direct e2e coverage (not
  just an incidental byproduct of other tests needing it to pass). e2e 9/11 passing → 12/12.

## (b) Improved
None shipped on this branch this pass — see the self-critique in (e): this run initially
re-implemented two BACKLOG items (#24, #26) plus the npm-audit CVE fix, only to discover via
open-PR diffing that PR #33 (`de/safety-20260910`, 4 days old, unmerged) already contains
functionally-identical fixes. Reverted the duplicates rather than ship a second, competing copy —
see (d) and (f) for what that means operationally.

## (c) Proposed for review
None new this pass.

## (d) Flagged (not implemented this pass — action already exists elsewhere or needs a human)
- **⚠️ CRITICAL, currently live on `main`, fix already written and unmerged.** `npm audit
  --omit=dev --audit-level=high` on `main` shows 1 critical + 1 high:
  `next@15.5.23` is in the vulnerable range for GHSA-p293-qw3h-jr36 (Next.js unauthenticated RCE,
  Windows-hosted servers) and GHSA-2xp9-vwfh-vxw4 (RCE in the Image Optimization API via AVIF
  files); the bundled `sharp` also carries the libvips/libheif high-severity CVEs
  (GHSA-f88m-g3jw-g9cj, GHSA-rgj7-g3m4-5g8c) from the same old `next` range. **This was already
  fixed 4 days ago**: PR #33 bumps `next` 15.5.23→15.5.25 (a patch-level version pin, closes all
  of the above; `sharp` moves 0.34.5→0.35.4 as a side effect) — it's just sitting unmerged.
  Exposure is likely low pre-merge (Vercel/Linux hosting, not Windows; no `next/image` usage, no
  `images.remotePatterns`) but a CRITICAL advisory with a ready, verified, zero-risk patch sitting
  4 days unmerged is the real issue here — see (f).
- **#25 (carried, unchanged):** `data:` base64 photo URLs (`app/api/listings/route.js:66`,
  `[id]/route.js:80`) bypass the upload pipeline's EXIF/GPS stripping entirely, stored verbatim.
- **#23 (carried, unchanged, needs Evan):** `app/api/stripe/checkout/route.js`'s dev-simulation
  branch has no production guard — monetization control flow, flag-only per the hard rules.
- **NEW — PR #12 (liability-terms, "HOLD for counsel") now merge-conflicts with `main`.**
  `git merge-tree` (clean as of 2026-09-07) now shows a 1-line conflict in
  `components/AuthForm.js`: PR #34's signup consent-gate checkbox (`const [agreed, setAgreed]`,
  merged 2026-09-13) landed on the same lines #12 touches for its own clickwrap-assent UI. Not
  urgent — #12 is an explicit hold pending counsel — but whoever picks it back up will need a
  rebase, and should check whether #12's clickwrap work is now partially redundant with what
  shipped in #34 (the click-through consent box exists; #12's remaining scope may be narrower —
  the actual liability/arbitration terms language, not the checkbox mechanism).

**Spot-audited (no findings):** the 2026-09-13 messaging-notification build (`0e23058`) —
`lib/notify.js` is fire-and-forget/guarded (a notify failure can't 500 the send), recipient email
is always fetched server-side from the DB (never client-supplied), both call sites
(`app/api/messages/[threadId]`, `app/api/threads`) retain their existing ownership/rate-limit/
block checks unchanged, and the burst-safety claim (at most one email per "catch-up" cycle) checks
out against the `readAt`-based query. The signup consent gate (`app/api/auth/signup/route.js`) is
enforced server-side, not just in the UI. `scripts/grant-admin.cjs` (PR #35, merged) is read-first
(LIST/DRY/WRITE), idempotent, refuses a `file:` DB without `--allow-dev`, never touches the
password. `scripts/signup-audit.cjs` (PR #36, open) is pure reads, same DB-safety guard.

## (e) Review verdict
**VERDICT: PASS, with a logged self-correction.** The one committed change (`7be6055`) passed the
full build+test gate before commit, is a pure test fix with zero application-behavior change, and
its claimed effect (e2e 9/11→12/12) is real, shown above. **What went sideways mid-run:** the
routine's own explicit warning ("before implementing an item, check whether it duplicates
already-completed — even if unmerged — work") got skipped — this run built a next@15.5.25 CVE fix,
the #24 `select`-scoping fix, and the #26 client-error field-allowlist fix independently, before
diffing against open PR branches. `git log main..origin/de/safety-20260910` showed PR #33 already
has functionally identical commits for all three, 4 days old. Caught it before opening a PR (not
after merge, so nothing bit-rotted or had to be reverted post-merge — better than the 2026-08-17
incident this exact rule cites, but the rule should have been checked *first*, not *after*
implementing). Corrected by `git reset --hard main` + re-cherry-picking only the genuinely-new
e2e fix, and re-pointing BACKLOG #24/#26/#27 at PR #33 instead of re-claiming the work. Schema
provider = postgresql (restored after every build), working tree clean, `main` not pushed.

## (f) STALE PRs
`gh pr list --state open` (ages computed from the device clock, 2026-09-14):

| PR | Title | Age | Status this pass |
|----|-------|-----|-------------------|
| #37 | brand: swap acid-yellow accent → cobalt blue | <1d | new, not audited here (Dev's own branch, pending Miles' contrast eyeball per the open-commitments ledger) |
| #36 | ops: signup-audit.cjs | <1d | still merges clean (0 conflicts), read-only tool, not stale |
| **#33** | **data-engineer: safety + improvements 2026-09-10** | **4d** | **Not yet "stale" by the >7d bar, but flagging urgently anyway: this PR is the only place the CRITICAL Next.js RCE fix + BACKLOG #24/#26 currently exist** (see (d)). Still merges clean (0 conflicts against current `main`). Recommend prioritizing this one over the general merge-backlog note below — a critical, verified, zero-risk patch sitting unmerged is the highest-leverage single action available right now. |
| ~~#28~~ | ~~marketing: UX review 2026-08-31~~ | 14d | **CLOSED this pass.** Live-reverified against current `main`: its one new finding (N1, `/pro` page's static "simulated" disclaimer) is resolved (`stripeConfigured()` now gates it, matching `/subscribe`); its carried #23 re-checks are covered by #23's own closure. Nothing lost — T3/T4 remain tracked in BACKLOG. |
| ~~#23~~ | ~~marketing: UX review 2026-08-24~~ | 21d | **CLOSED this pass** — 3rd consecutive run flagging it (2026-09-07 recommended closure, unactioned 7 more days). Live-reverified: T2 (pricing-copy conflict) and A1 (no founding-seller mention on /sell-tires) are both resolved on `main`; T3/T4 still open but already captured fresh in the merged `marketing-reports/2026-09-07.md` and tracked in BACKLOG. |
| #18 | SPIKE: Cloudflare/OpenNext port | 49d | Still an explicit Evan decision-hold. Bit-rot is worsening, not improving: `git merge-tree` now shows **34 conflict hunks** against current `main` (was flagged as freshly-conflicting on 2026-09-07; the gap has widened as `main` keeps moving). Not recommending closure — it's intentional — but the eventual rebase is getting more expensive every week this sits. |
| #12 | legal: liability terms — HOLD for counsel | 67d | Explicit hold, expected to sit — but **see (d): now has a real merge conflict** (`components/AuthForm.js`, from PR #34's signup consent checkbox), the first time this PR has conflicted. Flagging so the eventual pickup isn't a surprise, and so whoever scopes it checks for overlap with #34's already-shipped clickwrap UI. |

**Escalation, sharpened from the last 3 reports:** the standing "merge backlog" concern (flagged
2026-08-31, 2026-09-03, 2026-09-07) did see real movement since — #27/#29/#30 merged, #23/#28
closed this pass as superseded rather than left to rot further. But **#33 is a different and
more urgent case**: it isn't stale by the day-count yet, but it's carrying a CRITICAL,
already-fixed, zero-conflict, build+test-verified security patch that's been sitting 4 days.
Recommend merging #33 specifically, this week, ahead of the general triage-backlog cadence.

---
**Verification commands run:** `npm ci` (clean install matching lockfile) · `APP_SECRET=... npm
run build` (exit 0) · `npm test` (83/83) · `node --test e2e/flows.mjs` (12/12) · `npm audit
--omit=dev --audit-level=high` (on `main`: 1 critical + 1 high, unchanged by this branch — fix
lives on PR #33) · `git merge-tree` dry-run against every open PR branch · live smoke test
(`next start` + curl) confirming the reverted seller-profile/client-error files match `main`
byte-for-byte before closing out.
