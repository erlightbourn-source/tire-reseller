# TireTrader Safety + Bug Audit — 2026-07-02 08:38

**Branch:** `de/safety-20260702` (off `main`; `main` never touched)
**Audited commit (main HEAD):** `db2a884` ← record for the unchanged-code fast path
**Auditor:** `data-engineer` subagent (46 tool calls), independently reviewed
**Context:** `main` changed since the last audit (`cea2b06` → `db2a884`): PR #1 (soft-deleted-seller visibility filter) and PR #2 (`8b2be88`, per-recipient email rate cap on forgot/resend-verification — the fix for the previously flagged auth-email finding) both merged 2026-07-01. Because auth code changed, the full audit ran (fast path not applicable). Thursday — no Monday UX review. No open GitHub issues (gh-issues supplement: nothing to pick up); no open PRs.

---

## (a) Fixed — committed on `de/safety-20260702`

**Nothing committed this run.** The full sweep surfaced no auto-fixable (low-risk, localized, behavior-preserving) findings. No code changed, so no build/test gate was required; `npm audit` reports **0 vulnerabilities**.

### Priority re-review of merged `8b2be88` (auth-flow change) — CORRECT, approved

Independent trace through `app/api/auth/forgot/route.js`, `app/api/auth/resend-verification/route.js`, `lib/security.js:77`, `lib/validation.js:17`:

- The `key` option genuinely buckets on the normalized address (`lib/security.js:80` builds `forgot-addr:<addr>` / `resend-verify-addr:<addr>`), not the IP.
- No account-existence oracle: the per-address 429 fires **before** the `prisma.user.findUnique` lookup, so it behaves identically for existing and non-existing accounts.
- `isEmail(addr)` is validated before the limit call, so garbage input can't spray unbounded rate-limit buckets.
- Faithfully mirrors the existing `app/api/email-alerts/route.js:24` pattern (3/hour, keyed on addr, applied after validation, before side effects).

## (b) Flagged for review

1. **[Low — NEW] Reactivation restore threshold disagrees with auto-hide rule** — `app/api/auth/login/route.js:62` vs `app/api/reports/route.js:7`
   On soft-delete reactivation, listings are restored only if raw `_count.reports < 3`, but report auto-hide triggers at `AUTO_HIDE_AT = 5` **credible** (>24h-old account) reporters. A listing hidden purely by the soft-delete that has 3–4 raw reports (or ≥3 raw but <5 credible) stays incorrectly hidden after the owner reactivates. Fails conservative (over-hides, never under-hides), so low impact.
   - **Recommended fix:** recompute the credible-reporter count on reactivation and compare to `AUTO_HIDE_AT`, or export a shared `isAutoHidden(listingId)` helper from the reports module so the two code paths can't drift.
   - **Why human:** touches auth/account-flow behavior (reactivation) and moderation policy; the exact restore rule is a product decision.
2. **[Low — carried] Plaintext `email-alerts` confirm token** — benign opt-in action; unchanged, not worsened. Hash-at-rest like reset/verify tokens if hardening is ever desired.

## All-clear areas verified this run

Auth core (bcrypt-12 + SHA-256 prehash, httpOnly/sameSite/secure cookie, HS256 pinned, tokenVersion revocation, hashed reset tokens, APP_SECRET fail-closed); every auth route incl. constant-work login timing defense and atomic single-use tokens; ownership on every `[id]`/`[threadId]` route and page; offers/reviews/reports anti-abuse; CSRF + nonce CSP middleware; JSON-LD-only `dangerouslySetInnerHTML`; PII-scoped selects (no email/token leakage, coarse geo never serialized client-side); upload hardening (magic-byte, 8MB cap, EXIF strip); denorm sync (`sellerPro`, `ratingAvg/ratingCount`, `deriveListingColumns`); error/loading boundaries; rate limits on every sensitive mutating route; `npm audit` 0 vulnerabilities.

---

## (c) Review Verdict

**VERDICT: PASS**
**GRADE: A**

Independent re-read of this run's (empty) committed diff and the flagged list:

- **Nothing was committed, so nothing could over-claim.** The build/test gate was correctly not exercised — it gates commits, and there were none. Tree verified clean, schema line 9 = `provider = "postgresql"`.
- **The new flagged finding is real** — independently confirmed in source: `login/route.js` filters on raw `_count.reports < 3` while `reports/route.js` hides at 5 credible reporters. Correctly placed in the flag-only lane (auth-flow + moderation policy), not auto-fixed.
- **The 8b2be88 re-review was genuine** — the subagent traced the rate-limit key construction through both lib layers and checked oracle symmetry, rather than trusting the commit message.
- **Nothing auto-fixed that should have been flagged** — nothing was auto-fixed at all.

No fixes reverted. No REVISE actions.

---

## Addendum — improvement pass (same day, per Evan's "improve, don't just maintain" mandate)

Evan upgraded the routine mid-day (2026-07-02) and asked for the first improvement pass immediately. Executed on this same branch:

- **`177e8bd` (shipped, backlog #1):** e2e route-level tests locking the per-address email caps from 8b2be88 — 4th same-address forgot/resend-verification request in an hour → 429 + Retry-After, different address unaffected, neutral `{ok:true}` symmetry for an existing verified account. **Effect: e2e 7→9 tests.** Gate: build OK, unit 61/61, e2e 9/9.
- **`fafc652` (housekeeping):** fleet-standards header rolled into the in-repo agent definitions (docs-only).
- **`1566d13` (PROPOSAL, backlog #2):** reactivation listing-restore now uses the same credible-reporter auto-hide rule as the reports endpoint, via new shared `lib/moderation.js` (`AUTO_HIDE_AT`, `credibleReportCount`, `autoHiddenIds` — one grouped query, no N+1). **Decision for Evan:** restore threshold now equals the hide threshold (5 credible reporters); can be made stricter on request. Gate: build OK, unit 61/61, e2e 9/9 (exercises the modified login route), and the `groupBy` relation-filter query executed directly against dev.db (it has no seed coverage in e2e).

**Addendum review verdict: PASS / A-.** Each commit passed the full gate individually (item #2 was stashed while gating item #1). Residual weakness, stated honestly: no automated test covers the reactivation-restore path itself (needs a soft-deleted seller with aged reporters — beyond small-diff seed surgery this run); the proposal's behavior change is gated by Evan's PR review, which is the designed control. Backlog updated: #1 shipped, #2 proposal, new launch-readiness item at 2.5 per the launch mandate. Code-side launch blockers known: 0.
