# TireTrader Safety + Improvement Audit — 2026-07-16 08:37 ET

**Branch:** `de/safety-20260716` (off `main`; `main` never pushed)
**Audited commit (main HEAD):** `c688880` ← record for the unchanged-code fast path
**Auditor:** Dev, FAST-PATH run (`main` HEAD unchanged since the 2026-07-13 audit, which had no open critical/security flags — only a product-decision flag on the recovered Founding-Seller WIP and carried low-severity items). Freed effort spent on step 3b per the fast-path rule (up to 3 backlog items). Independent reviewer spawned via the Agent tool (two-expert gate) on this run's diff. Thursday — no Monday UX-review add-on this run.

---

## (a) Fixed — committed on `de/safety-20260716`

**None.** Abbreviated audit only (fast-path): `npm audit` = **0 vulnerabilities** (unchanged), local Node is now v24.18.0 (above the ≥22.22.0 patched line — Ada's intel confirms this is already the patched Node release, no action). Carried flags re-checked and still present, unchanged (see §(d)).

## (b) Improved — shipped this run

**`661a83c` + `c7a2ecc` (BACKLOG #5) — loading boundaries on `/subscribe/success` and `/unsubscribe`.**
Both routes ran a real server-side async operation (Stripe session retrieve + DB write on the Stripe-redirect success page; a DB token lookup on unsubscribe) with no loading boundary, so a slow request showed a blank screen — the same class of gap the 2026-07-13 run fixed on `/messages`. Verified neither route calls `notFound()` (both are redirect-or-conditional-render only), so no soft-404 risk from the 200-shell a boundary flushes.
- **Measurable effect:** routes with a loading boundary **6 → 8** on `main` (the 7th, `/messages`, still rides open PR #15).
- **Verified:** `APP_SECRET=… npm run build` OK (both routes compiled); `npm test` **61/61** after each commit; `prisma/schema.prisma` line 9 restored to `provider = "postgresql"` after each build.

Backlog refreshed (`af45e25`, corrected `d1b183b`): #5 → shipped; three new entries opened from this run's inspection — #5a (homepage needs a `<Suspense>`-per-section restructure, not a page-level `loading.js`; needs-plan), #5b (browse radius-search `take: 2000` path — measure before touching), #5c (Stripe `apiVersion` pinned 18 months stale — flag only, payments surface).

## (c) Proposed for review — committed, needs Evan's PR review

**None new this run.** (Prior proposals ride their own still-open PRs — confirm-token-hash on #13, soft-delete-threshold on #3.)

## (d) Flagged for review (description-only — need a human/product decision)

1. **[HIGH — product decision, carried] The recovered Founding-Seller WIP** (`de/founding-seller-wip-20260713`, untouched this run) — still awaiting Evan's go/no-go + a proper Prisma migration.
2. **[Med — launch-readiness, carried, BACKLOG #15] Dev/demo UI leaks to production** (`AuthForm.js:206`, `app/pro/page.js:57`) — still present, unchanged. Needs a `NEXT_PUBLIC_DEMO_MODE` flag as a PROPOSAL when Evan wants it.
3. **[Med — payments, NEW, BACKLOG #5c] `lib/stripe.js:25` pins `apiVersion: "2024-11-20.acacia"`** — Ada's intel has current Stripe at `2026-06-24.dahlia`, with a breaking change at `2026-03-25.dahlia` (Payment Element only, legacy Stripe.js methods removed) in between. No live traffic yet, so no incident today, but flag-only per the hard rules (payments/billing surface) — needs Evan's call on when to re-pin + a Payment-Element-only check before go-live.
4. **[Med — ops, carried, BACKLOG #3c] Local dev Node.** No longer behind — this run's build ran on Node **v24.18.0** (above the ≥22.22.0 patched line). Still confirm the Vercel/deploy Node is ≥22.22.0.
5. **[Low — carried, BACKLOG #3a] Unsubscribe token plaintext at rest** — needs a per-email HMAC redesign (needs-plan), unchanged.
6. **[Low — carried, BACKLOG #17] "Verified Pro" overstates what's verified** — copy softening is safe; real verification tied to the Founding Seller promo is a product decision.
7. **[Low — perf, NEW, BACKLOG #5b] `app/browse/page.js:94` radius-search path fetches `take: 2000`** — not a regression (deliberate DB-pagination exception), but worth measuring real request volume/latency before optimizing.

## All-clear areas (fast-path re-check, not a full re-audit)

- `npm audit` = **0** (unchanged from 2026-07-13).
- Carried security-floor items (authZ/IDOR, auth/session, XSS, SQL, rate limiting, upload hardening, cron/webhook auth) were **not re-walked in full this run** (fast-path scope) — the 2026-07-13 full audit + independent reviewer verified them clean, and nothing in this run's diff (two one-line UI re-export files) touches any of that surface.
- Stripe API version drift (§(d)3) is the one genuinely new finding from this pass, not a floor breach (no live Stripe traffic).

**Launch blockers remaining (code-side): 0** — unchanged from 2026-07-13. Founding-Seller WIP is a new-feature decision, not a blocker on the current build.

---

## (e) Review Verdict

**VERDICT: PASS**
**GRADE: A−**

Independent reviewer spawned via the Agent tool (two-expert gate — per this OpenClaw host's standing rule, run as a synchronous in-turn Agent call rather than a spawn-and-block session, since the latter is known to hang this surface) re-read both commits and the BACKLOG diff against the live files, and independently ran the build + test suite.

- **Both shipped changes are genuinely additive and correctly scoped.** Reviewer confirmed neither `/subscribe/success` nor `/unsubscribe` calls `notFound()`, confirmed the `@/components/PageLoading` import resolves and matches the byte-identical convention used by the 5 existing `loading.js` files, and independently re-ran build (pass, both routes compiled) + tests (61/61) + confirmed the schema provider was restored to `postgresql` afterward.
- **One inaccuracy was caught and fixed before this report was finalized:** the first draft of BACKLOG #5a claimed the homepage hero was "fully static, no DB dependency, paints immediately." The reviewer traced `app/page.js:96-97` and showed the hero's `{totalActive}`/`{stateCount}` actually come from the same awaited `Promise.all` as the listings/brands queries — the whole page already blocks together today, not just the data sections. Corrected in `d1b183b` before this report shipped. This is exactly the kind of claim the two-expert gate exists to catch: it didn't affect the two shipped commits (neither touches `app/page.js`), but it would have mis-scoped whoever picks up #5a next.
- **Nothing auto-fixed that should have been flagged or proposed.** The Stripe API-version drift (new this run) was flagged, not touched — it's payments-surface and explicitly flag-only per the hard rules.
- **Reconciliation:** one round — the reviewer's finding was accepted and fixed directly (no rebuttal needed, the evidence was unambiguous). **AGREED**, no unresolved disagreement.
- **Honest residual:** this was a fast-path run — the security floor (authZ/IDOR/auth/XSS/SQL/rate-limiting/upload/cron) was NOT independently re-walked this time, only `npm audit` + the carried-flag recheck. That's the deliberate fast-path trade (this run's diff doesn't touch any of that surface), but it means this report's confidence on the floor rests on the 2026-07-13 full audit, not a fresh one.

No fixes reverted. No REVISE actions.

## (f) Stale PRs (>7 days)

**`#13` `de/safety-20260709`** — opened 2026-07-09, now **7 days old**, carries the confirm-token-hash proposal. At the edge of the window — worth a review pass this week.
**`#12` `de/liability-terms-20260709`** — opened 2026-07-09, now **7 days old**, explicitly on HOLD for counsel review (by design, not neglect).
Within-window: **#15** `de/safety-20260713` (3d), **#14** `mktg/review-20260713` (3d).
