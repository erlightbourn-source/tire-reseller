# TireTrader safety + improvement pass — 2026-09-21

**Audited commit (main HEAD):** `d02d314` (branch `de/safety-20260921` cut from it).

HEAD had moved since the last full audit (2026-09-14 report, audited `3a803c0`) via 4
merged PRs not previously covered by this routine (state-capture + geocode fallback #41,
case-insensitive Postgres search, and the two PRs those built on) — ran as a full pass,
not the unchanged-code fast path.

## Housekeeping (pre-audit)

- `npm audit --omit=dev --audit-level=high` → **0 vulnerabilities** on current main.
- `gh pr list --state open` at run start: PR #42 (`de/safety-20260917`, 4 days old) was
  the only open PR. See the process-miss note below — it's directly relevant to this run.

## (a) Fixed

None. Full re-audit of everything changed since `3a803c0` (auth forms, listing create/edit
routes, geo/state resolution, upload/R2 storage, listingFilter's Postgres case-insensitivity
gate, the hardened legal terms) found no new correctness or security bugs — every new
surface traced cleanly: `resolveState`/`stateFromLocation` only ever return a value from
the fixed `STATES` allowlist (no injection/prototype-pollution path, verified the
`STATE_CENTROIDS[stAbbr]` lookup is safe because `stAbbr` is uppercased before the lookup,
which neutralizes a `__proto__`-style key since real prototype-chain keys are lowercase),
`/api/upload` validates real image bytes + size + auth + rate limit before storing,
`isAllowedPhotoUrl`'s R2/blob host allowlists are prefix-anchored (no lookalike-domain
bypass), and every `[id]`/`[threadId]` route re-confirmed to enforce ownership before
returning data. `npm audit` clean.

## (b) Improved

- **`186d74e`** — closed the remaining sub-item of BACKLOG #2.5 (launch-readiness):
  built `e2e/app-secret-boot.mjs`, wired into `npm run test:e2e`, which spawns a real
  `next start` in production mode with a deliberately-weak `APP_SECRET` and empirically
  proves what the boot-behavior actually is (the original backlog item assumed a
  process-boot-time throw — that assumption was wrong): `/api/health` stays 200 (it only
  touches `lib/db`, never `lib/auth`), while an auth-touching route (`/api/account`) 500s
  only on its *first hit*, not at boot. A control test with a valid secret confirms the
  same route returns a normal 401, proving the weak-secret assertion is measuring the
  right thing. **Measurable effect:** 2 new e2e tests, both green; existing e2e suite
  (13 tests) unaffected, 15/15 combined. Test-only, no behavior change — the actual gap
  (a health-only monitor wouldn't catch this) is flagged below for a human call.

## (c) Proposed for review

None this run.

## (d) Flagged

1. **PROCESS MISS, not a code bug — but worth Evan seeing.** This run independently
   re-implemented BACKLOG #25 (data-URI photos bypass EXIF/GPS stripping) before checking
   `gh pr list --state open`, not realizing PR #42 (open since 2026-09-17, `71bb3c6`)
   already shipped the identical fix to a branch, tested and ready. Caught it during step
   4's stale-PR check, reverted the duplicate (`e72c9b9`) rather than leave two divergent
   fixes for the same finding on two branches. Root cause: step 3b's "check whether it
   duplicates already-completed (even if unmerged) work" instruction says to check PR
   diffs, not just BACKLOG.md's status column — I checked BACKLOG.md (which still said
   "flagged," accurate as of main but stale relative to the open PR) and skipped the PR
   check. **No code impact** (the duplicate was reverted before this branch's PR was
   opened), but it's the second time this exact failure mode has been named in this
   routine's history (see the 2026-08-17 incident this SKILL already documents) — worth a
   standing habit fix: run `gh pr list --state open` and skim diffs for touched files
   *before* implementing, not just at report time.
2. **ACTION FOR EVAN: merge PR #42.** It's a clean, tested, already-reviewed fix
   (BACKLOG #25, `lib/image.js#stripDataUriMetadata`, suite 90→94) sitting open 4 days
   against the current `main` HEAD (no rebase needed — cut from the same commit this run
   started at). Nothing further for this routine to do here; merging is the only
   remaining step.
3. Carried-forward, unchanged: BACKLOG #23 (Stripe dev-simulation branch has no deploy
   guard — monetization, needs Evan), and the `/api/health` doesn't cover a missing/weak
   `APP_SECRET` gap surfaced by this run's own new e2e test (see item (b) above) — fixing
   it means changing auth-flow/bootstrap behavior, out of this routine's auto-fix scope.

## (e) Review verdict

**VERDICT: PASS**, with one self-caught process miss (see (d)1) that was corrected within
this same run before anything shipped. The one committed improvement (`186d74e`) is
test-only, passed the full e2e suite (15/15) both before and after, and its claimed effect
(documents a real, previously-unverified boot-behavior gap) is directly demonstrated by
the new tests rather than asserted. `prisma/schema.prisma` confirmed restored to
`provider = "postgresql"`; working tree clean; `main` not pushed.

## Verification

- `APP_SECRET=dummy_ci_secret_for_build_only_32chars_long npm run build` → clean, exit 0.
- `node --test` (unit suite) → 91/91 pass.
- `npm run test:e2e` (`e2e/flows.mjs` + `e2e/app-secret-boot.mjs`) → 15/15 pass.
- `npm audit --omit=dev --audit-level=high` → 0 vulnerabilities.
- `prisma/schema.prisma` line 9 → `provider = "postgresql"` (confirmed post-build).

## (f) STALE PRs

- **#42** (`de/safety-20260917`, "data-engineer: safety + improvements 2026-09-17") — 4
  days old, not yet past the 7-day stale threshold, but flagged above under (d)2: it's a
  clean, tested, ready-to-merge fix cut from the current `main` HEAD. Recommend merging
  now rather than letting it age into "stale."
- No PR >7 days open (the prior recurring offenders — #33, #37–#41 — are all merged).
