# TireTrader Safety + Improvement Report — 2026-09-07

**Audited commit (main HEAD):** `37ed302` (branch `de/safety-20260907` cut from it).

Not a fast-path run: HEAD has moved substantially since the last full audit whose report was
actually merged (2026-08-27 report, audited commit `55b5d1e`, itself unchanged since 2026-07-06) —
main has since absorbed the demo-admin-backdoor removal (`5b66da6`/PR #26), SEO plumbing, and
yesterday's one-plan/two-price rewrite (`803de91`). Ran the full audit.

## (a) Fixed
None. No new security/correctness bugs found this pass (see audit scope below).

## (b) Improved
- **`test/seller-plan.test.mjs` — cover `priceConfigState`'s "mixed" branch** (`d0ba853`,
  BACKLOG #18). Auditing yesterday's pricing rewrite (`803de91`) found `tiered`/`legacy`/`partial`/
  `none` states tested but not `mixed` — where one tier's Stripe price env var is set alongside the
  legacy `STRIPE_PRICE_ID` and the *other* tier silently bills at the legacy price. `lib/stripe.js`
  already warns on this at startup; this locks in the actual behavior with a test (2 new assertions
  per direction) so a future refactor can't silently change it unnoticed. Measurable effect: unit
  suite 71→72, zero production code touched. Pure test addition, no schema/monetization edit.

## (c) Proposed for review
None this pass.

## (d) Flagged (not implemented)
- **`sharp`/`next` — carried `npm audit` high-severity, unchanged.** `sharp <0.35.0` pulls
  vulnerable `libvips` (CVE-2026-33327/33328/35590/35591); the only fix path (`npm audit fix
  --force`) bumps `next` outside its stated range (to 15.5.25) — a minor/major dependency bump per
  the hard rule, flag-only. Carried since 2026-08-20/24 reports, still the only open `npm audit`
  item at `--omit=dev --audit-level=high`.
- **BACKLOG T3 (from today's marketing review) — "verified"/"trusted" seller-badge copy vs. no real
  verification step.** Business decision (soften copy vs. build real verification), not a code bug.
- **BACKLOG T4 (from today's marketing review) — `app/listings/[id]/page.js:237` hardcodes "Usually
  responds within a day"** regardless of actual reply behavior. Needs a query-design decision
  (rolling window vs. cached average) before it's data-engineer-safe; not urgent pre-launch with
  ~0 real sellers, but should not ship to real sellers unfixed.

**Spot-audited (no findings):** yesterday's Stripe/pricing rewrite (`803de91`, 32 files) — webhook
signature verification still intact and unchanged (`app/api/stripe/webhook/route.js`), `/api/pro`
and `/api/stripe/checkout` retain auth + `getCurrentUser()`/`canSell()` gating with a fail-closed
production guard on the dev-simulation branch, `/api/admin`'s `makeFounding`/`unmakeFounding`
actions are behind `isAdmin()` + rate limit + audit log (same as pre-existing admin actions — no new
IDOR surface). `app/api/listings/[id]` and `app/api/messages/[threadId]` ownership checks
unchanged and still test-covered. No new `dangerouslySetInnerHTML` sinks (all existing ones are the
audited `jsonLdHtml()` escaping helper or static icon-map constants, not user input).

## (e) Review verdict
**VERDICT: PASS.** The one committed change (`d0ba853`) passed the full build+test gate — shown
below, not asserted — before commit. Nothing was auto-fixed that should have been a proposal or
flag instead (pure test, zero app-behavior change, not auth-flow-adjacent). Its claimed effect
(unit 71→72, locks in an already-correct-but-unverified code path) is real in the diff, not
inflated. Build clean · unit 72/72 · e2e 10/10 · `npm audit --omit=dev --audit-level=high` = 1
carried flag (unchanged) · schema provider = postgresql restored · working tree clean · `main` not
pushed.

## (f) STALE PRs
`gh pr list --state open` (ages computed from the device clock, 2026-09-07):

| PR | Title | Age | Status this pass |
|----|-------|-----|-------------------|
| #30 | data-engineer safety+improvements 2026-09-03 | 3d | still merges clean, not yet stale |
| #29 | cleanup: purge abandoned unverified signups | 6d | still merges clean, not yet stale |
| #28 | marketing: UX review 2026-08-31 | 6d | still merges clean, not yet stale |
| #27 | data-engineer safety+improvements 2026-08-31 | 6d | still merges clean, not yet stale |
| **#23** | **marketing: UX review 2026-08-24** | **14d** | **SUPERSEDED — recommend closing.** Its own 4 findings (T2/T3/T4/A1) are now covered by this run's `marketing-reports/2026-09-07.md`: T2/N1/A1 confirmed resolved on `main`, T3/T4 re-flagged fresh. Closing #23 loses nothing — everything in it is either landed or carried forward in a newer report. |
| #18 | SPIKE: Cloudflare/OpenNext port | 42d | Evan decision-pending (explicitly "needs a path decision" in its own title); local `git merge-tree` now shows **CONFLICTS** against current `main` (did not conflict as of the last check) — will need a rebase whenever the path decision is made. Not recommending closure — it's an intentional hold, not neglect — but flagging the bit-rot so the eventual rebase isn't a surprise. |
| #12 | legal: liability terms | 60d | Explicit "HOLD for counsel" in its own title — expected to sit, not a neglect signal. Still merges clean. |

**⚠️ Escalating again: the real safety/improvement backlog (#27, #29, #30) is now 3 PRs and up to
6 days old, on top of #28's UX findings sitting the same length of time — this is the third
consecutive run flagging the merge backlog (2026-08-31, 2026-09-03, now 2026-09-07) with no
merges in between.** Content-wise nothing has bit-rotted (all three still merge cleanly against
current `main`), but every day they sit is a day the shipped security proposals in #27 aren't
actually live. Recommend a batch merge/triage pass rather than another audit-only cycle.

---
**Verification commands run:** `APP_SECRET=... npm run build` · `npm test` (72/72) ·
`node --test e2e/flows.mjs` (10/10) · `npm audit --omit=dev --audit-level=high` · `git merge-tree`
dry-run against each open PR branch.
