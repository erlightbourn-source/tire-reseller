# TireTrader safety + improvement pass — 2026-09-03

**Audited commit (main HEAD):** `7243044` ← unchanged since PR #27's 2026-08-31 full
audit branched from it (confirmed: `git merge-base origin/main origin/de/safety-20260831`
== `7243044`, and `git log origin/de/safety-20260831..origin/main` is empty — nothing
has landed on `main` since). Per the fast-path rule: **ABBREVIATED audit** this run,
freed effort spent on BACKLOG #2.5 (below) and a hard look at the open-PR backlog.

## (a) Fixed
None auto-fixed this run — the abbreviated audit found nothing new (see Verification).

## (b) Improved
1. **`5d0600d`** — BACKLOG #2.5 (CRON_SECRET sub-item), **shipped**. Added an e2e test
   locking in that `/api/cron/alerts` fails closed without `CRON_SECRET`, mirroring the
   test `/api/cron/purge` already had. Both routes share the same `bearerMatches()` gate
   in `lib/security.js`; only one call site had regression coverage, so a future change
   to the shared gate could have silently opened the alerts digest endpoint (reads every
   saved search + emails users) without either existing suite noticing. Measurable
   effect: e2e 10 → 11, both cron routes now independently covered.

## (c) Proposed for review
None new this run — see PR #27 (still open, unmerged since 2026-08-31) for 5 already-
proposed, already-verified security commits (host-header-poisoning password-reset fix,
prod seed guard, 5-account demo neutralization, per-account rate-limit fix, PNG/WebP
EXIF stripping). Nothing to duplicate; see escalation below.

## (d) Flagged
Carried, unchanged since PR #27 (2026-08-31) — re-confirmed still accurate against
current `main`:
- **HIGH — Stripe checkout dev-simulation branch** (`app/api/stripe/checkout/route.js:17-27`):
  no `NODE_ENV`/config guard — if Stripe env vars are unset or placeholder in prod, any
  logged-in user hitting `/api/stripe/checkout` gets `subscriptionStatus: "active"` for
  free (`dev_simulated`). Confirmed today the code path is unchanged and the local
  `.env` still ships placeholder-shaped values in `.env.example`; I cannot check the
  live Vercel env from here. **This was flagged "check today" on 2026-08-31 — it is now
  2026-09-03 with no evidence it's been checked.** Billing-adjacent → flag-only per hard
  rule, not something I can fix as a proposal.
- LOW/latent — `app/sellers/[id]/page.js:19` Prisma include without `select` pulls
  another user's passwordHash/email/tokens into the server render (not leaked today).
- LOW — `data:` base64 photo URLs bypass EXIF/GPS stripping.
- LOW — `app/api/client-error/route.js:30-34` spreads unauth request JSON into the
  outbound Slack webhook (field-injection risk).
- HIGH informational — `sharp`/libvips CVEs (GHSA-f88m-g3jw-g9cj) — re-confirmed via
  fresh `npm audit --omit=dev --audit-level=high` this run, unchanged, low practical
  exposure (no `next/image` usage). Fix path `npm audit fix --force` bumps `next` to
  15.5.25 (minor) — flag-only per dependency-upgrade policy.

**NEW this run — a live production exposure, not previously flagged in a safety
report:** `app/api/admin/neutralize-demo/route.js` (added by PR #25, merged +
deployed to prod via the `7243044` "trigger Vercel redeploy" commit on 2026-08-31) is
a standing, token-guarded, password-rewriting admin endpoint. PR #25's own body
committed to removing it once its one-time job was done; PR #26 (open since
2026-08-30, `de/remove-neutralize-endpoint-20260830`, clean/mergeable) does exactly
that, and states in its own body "Merge this with/before the Monday pipeline fix so
the endpoint never goes live" — Monday has passed, the endpoint IS live (it shipped
via 7243044 the same day #26 was opened), and #26 is still unmerged 4 days later. This
is the same shape of risk as the 2026-08-27 "unmerged safety-PR live exposure" finding,
recurring on a different endpoint.

## (e) Review verdict
**PASS.** The one committed change (`5d0600d`) is test-only, zero app-behavior change,
independently re-verified (build clean, 66/66 unit, 11/11 e2e including the new test).
Nothing was auto-fixed that should have been a proposal or flag; the Stripe finding
correctly stayed flag-only. Fast-path was the right call — `main` genuinely hasn't
moved since PR #27's thorough audit 3 days ago, so a second full audit would have
re-discovered the same 5 findings PR #27 already proposed fixes for, which is the
exact "new PR pileup instead of merging the last one" pattern flagged in past reports'
`improve:` lines.

**Environment note (not a code finding, logged for the record):** the first e2e run
this pass failed 1/11 ("seeded user can log in") because the local `prisma/dev.db`'s
demo-account password hash didn't match `demo1234` — a stale local seed artifact
(gitignored, machine-local, unrelated to any commit). Re-running `node prisma/seed.js`
against the local sqlite fixed it; all 11 passed after. Not a repo/report finding, but
worth a standing habit: reseed before trusting a red e2e result rather than assuming
the app broke.

## (f) Stale/at-risk PRs
Open PRs as of 2026-09-03, `gh pr list --state open`:
- **#27** `de/safety-20260831` (3 days) — 5 verified security proposals, still unmerged.
  Not "stale" by the 7-day yardstick but severity-escalating: this is real, tested
  security work sitting exposed while `main` stays vulnerable to what it fixes
  (host-header password-reset poisoning, 4 more publicly-credentialed seeded accounts,
  IP-rotation rate-limit bypass).
- **#26** `de/remove-neutralize-endpoint-20260830` (4 days) — removes a live, deployed,
  standing admin-write endpoint per its own "must merge before it goes live" commitment,
  which has now been missed. See (d) above — recommend prioritizing this over age-based
  staleness, given the endpoint is live today.
- **#29** `de/purge-unverified-signups-20260831` (3 days), **#28** `mktg/review-20260831`
  (3 days) — not stale, no action needed yet.
- **#23** `mktg/review-20260824` (10 days, STALE) — pure markdown report, no code/schema
  touched since, no bit-rot risk. Recommend merging; it's zero-risk and has sat 3 days
  past the 7-day mark for no evident reason.
- **#18** `de/cf-port-spike-20260719` (46 days) — explicit product-decision hold
  (Cloudflare-port spike); correctly held, no action.
- **#12** `de/liability-terms-20260709` (56 days) — explicit "HOLD for counsel"; correctly
  held, no action.

**Pattern flag (recurring across 2026-08-24, -27, -31 reports' `improve:` lines and
again here):** this cron keeps auditing and proposing correctly; the bottleneck is
consistently that PRs don't get merged, not that the audits are missing anything. Six
of seven open PRs are Dev's own; none merged in the last 4 pass cycles. Recommending,
again, that PR review/merge — not another audit pass — is the highest-leverage next
action on this repo.

Full detail on the carried findings: `safety-reports/2026-08-31-0859.md` (PR #27).

**Launch blockers remaining: 1** (Stripe env fail-open check — Evan, unresolved 3 days).

🤖 Dev (scheduled tiretrader-data-engineer-safety run)
