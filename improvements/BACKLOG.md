# TireTrader Improvement Backlog

Ranked queue for the scheduled safety+improvement routine (step 3b). Each run implements the top 1–2 items
(up to 3 on a fast-path run), one commit per item, build+test gated, ≤ ~150 changed lines each.
Statuses: `pending` · `in-progress` · `shipped <commit>` · `proposal <commit>` (awaiting PR review) · `needs-plan` · `dropped <why>`.
Never in scope here: schema migrations, monetization/pricing, GitHub Actions files, dependency major/minor upgrades.

LAUNCH WEIGHTING (Evan, 2026-07-02 — TireTrader launches THIS YEAR): rank launch-readiness
items (GO-LIVE.md gaps, Stripe live-mode readiness, deploy/env prep, seller onboarding
hardening) above nice-to-haves. Code-side launch blockers known: **0** — the 24 unchecked
GO-LIVE.md items are provisioning/ops (Neon, Vercel, Upstash, Resend, Stripe keys, migrate,
smoke test) owned by Evan; legal pages (terms/privacy) exist. Stripe live-mode and the
deploy are Evan's calls — prepare, never pull the trigger.

| rank | item | category | source | risk | status |
|------|------|----------|--------|------|--------|
| 1 | Route-level tests locking the per-address email caps (`forgot-addr`, `resend-verify-addr` from 8b2be88): 4th same-address request in an hour → 429, different address unaffected, neutral `{ok:true}` symmetry for existing vs non-existing accounts | test-coverage | audit 2026-07-02 (change merged untested at route level) | none | shipped 2026-07-02 (PR #3, e2e 7→9) |
| 2 | PROPOSAL: align soft-delete reactivation restore rule with the auto-hide rule — `app/api/auth/login/route.js` restores at raw `_count.reports < 3`, `app/api/reports/route.js` hides at 5 credible (>24h) reporters; shared `lib/moderation.js` used by both. Decision point for Evan: exact restore threshold | correctness / security-hardening | flagged finding 2026-07-02 | low (auth-flow → PROPOSAL commit) | proposal 2026-07-02 (PR #3) |
| 2.5 | Launch-readiness sweep: extend e2e smoke to mirror GO-LIVE.md §5 (list a tire → message → offer accept — **shipped 2026-08-27 via #4**), verify fail-closed prod paths (APP_SECRET, CRON_SECRET, Upstash warning), Stripe webhook test-mode dry run. **2026-09-03: CRON_SECRET sub-item shipped** (`5d0600d`) — `/api/cron/alerts` now has the same fail-closed e2e coverage `/api/cron/purge` already had; a shared-gate regression can no longer slip past unnoticed on one route while the other's test stays green. **Remaining, still open:** APP_SECRET is a process-boot-time throw (`lib/auth.js`), not exercisable via this per-request e2e harness — needs a separate smoke-test lane (spawn `next start` with a missing/weak APP_SECRET + `NODE_ENV=production`, assert non-zero exit) if this is worth automating vs. a documented manual GO-LIVE.md check; Upstash-warning and Stripe-webhook-dry-run remain manual ops-checklist items, not code | launch-readiness | launch mandate 2026-07-02; CRON_SECRET sub-item shipped 2026-09-03 | low | in-progress (2 of 4 sub-items shipped) |
| 3 | Hash the email-alerts CONFIRM token at rest, matching the reset/verify token pattern | security-hardening | carried finding since 2026-07-01 | low (auth-flow → PROPOSAL) | proposal 2026-07-06 (PR pending) |
| 3a | Harden the UNSUBSCRIBE token: it's a persistent capability the alerts cron re-emits per digest, so it can't be hashed at rest — needs a per-email HMAC (store a secret, sign `alertId` per send) so a leaked row can't be replayed. Design change → needs a plan | security-hardening | 2026-07-06 scoping of finding #2 | low | needs-plan |
| 3b | Build the missing token/email test lane: a dev-only email outbox (in-memory ring buffer in lib/email.js, non-prod gated) + a Prisma sqlite integration-test runner separate from the pure `node --test` suite, so route+DB+email flows (reset, verify, confirm, unsubscribe) get real automated regression tests. Unblocks proper testing of #3/#3a/#4 | test-coverage / infra | 2026-07-06 (confirm-token change had no wireable test) | low-med | needs-plan |
| 3c | Node runtime CVEs (CVE-2025-55131/55130/59465, HIGH) fixed in ≥22.22.0. `engines.node >=22.22.0` pin ADDED (adadc71) — Vercel reads it to pick the deploy runtime. **Remaining for Evan (ops):** confirm the Vercel/deploy Node is ≥22.22.0 and bump local dev Node (currently 22.14.0, emits a harmless EBADENGINE warning). `npm audit` = 0 (runtime, not dependency, CVEs) | security-hardening / launch-readiness | Ada intel 2026-07-06 | low | pin shipped 2026-07-08 (adadc71); deploy/dev runtime update = Evan |
| 4 | Expand `e2e/flows.mjs` coverage for the core funnel: signup → verify → list a tire → buyer message → offer accept. **2026-08-27: SHIPPED** (`83c09ff`) — one new `node:test` covering the full path plus a thread-authorization check (non-participant denied) and the accept route's already-answered/double-accept guard. Verify-token bypass done via a direct Prisma write (same effect as clicking the emailed link) since there's no dev email outbox yet (#3b blocks a cleaner version). e2e 9→10, all green. (Not Playwright — this repo's e2e lane is `node:test` + `next start` + fetch, no browser needed for API-level funnel coverage.) | test-coverage / resilience | audit 2026-07-02 (single e2e file) | none | shipped 2026-08-27 (83c09ff) |
| 5 | Error/loading boundary gap check: 7 `loading.js` exist — enumerate routes without one (or without empty-state handling) and fill the top gaps. **2026-08-20: NARROWED, not a simple win.** Live-verified (next start -p + curl -D) that loading.js on any segment whose page — or ANY descendant page — calls `notFound()`/`redirect()` degrades a real 404/307 to a soft-200 (Next.js commits the 200 status when the streaming Suspense boundary opens, before the async component can throw). This kills the item for `listings/[id]`, `messages` (parent of `messages/[threadId]`), `messages/[threadId]`, `sellers/[id]`, `tires/[brand]` — the exact routes that had real DB fetches worth a loading state. `sell`, `sell-tires`, `pro`, `subscribe`, `sell/bulk` (the routes the 2026-08-17 report flagged) turned out on inspection to have no real `await prisma...` calls — near-instant, low value for a loading.js anyway. Remaining real path: a client-side loading affordance (e.g. `useLinkStatus` / a top nav-progress bar) that doesn't touch the server response, not a route-segment `loading.js`. See `components/PageLoading.js` comment + safety-reports/2026-08-20-*.md. | resilience | audit 2026-07-02; re-scoped 2026-08-20 | none (investigation only; all drafted loading.js reverted) | needs-plan (client-side approach) |
| 6 | Accessibility pass on browse + listing detail (labels, alt text on listing photos, focus states, contrast). **2026-08-24: browse-filter labeling gap fixed** (941620f). **2026-08-27: listing-detail focus/contrast audited, CLOSED — no gap found.** Every interactive element on `app/listings/[id]/page.js` and its shared components (`PhotoGallery`, `FavoriteButton`, `MessageSeller`, `ReportListing`, `ShareListing`, `DeleteListingButton`) is a real `<a>`/`<button>`, not a `<div onClick>`, so all are already caught by the global `a:focus-visible, button:focus-visible, ...` 3px acid-yellow ring in `app/globals.css:18-27` — no div-click anti-pattern to fix. Contrast quantitatively checked (WCAG relative-luminance calc, not eyeballed): `text-slate-400`/black 8.2:1, `text-slate-300`/black 14.1:1, the amber "before you buy" box (`text-amber-100/80` over `bg-amber-500/5` on black, `text-amber-200` heading) 11.4:1 / 16.1:1 — all comfortably above the 4.5:1 AA floor. Item fully closed; no code change needed. | accessibility | own inspection 2026-07-02; browse fix 2026-08-24; listing-detail closed 2026-08-27 | none | shipped 2026-07-06/2026-08-24 (browse) + closed-no-gap 2026-08-27 (listing detail) |
| 7 | Query-efficiency check on homepage/browse feeds (N+1s, unbounded takes, missing indexes worth flagging) — measure before changing | performance | own inspection 2026-07-02 | low | pending — 2026-08-20 spot-check: no new N+1s in `app/page.js`/`app/browse/page.js`; the one `take: 2000` (radius-search bounding-box prefilter, `app/browse/page.js:94`) is deliberately capped and already commented (SQLite has no geo functions), not an unbounded query. No production load data to prioritize further without guessing — stays pending. |
| 15 | `next`/`postcss` patch bump closing 4 high-sev `npm audit` CVEs. **2026-08-24: landed on this branch** (cherry-picked `63ef440` from PR #21, which was sitting unreviewed 7 days) — `npm audit --omit=dev --audit-level=high` now shows only the pre-existing, correctly-flagged `sharp` minor-bump item below. Build clean, 66/66 tests pass. Still needs Evan/a reviewer to actually **merge this PR into `main`** — the fix only reaches prod once that happens. | security-hardening | npm audit 2026-08-20 (re-confirmed); landed 2026-08-24 | none (merge-only) | shipped-to-branch 2026-08-24 (ee7afc6); prod-live pending PR merge |
| 8 | Render the existing `SAFETY_WARNING` (+ `detectOffPlatform`) from `lib/safety.js` inside `components/MessageSeller.js` — the off-platform-scam warning already shows on the listing description but NOT in the composer, the exact moment a buyer is most exposed. Pure UI wiring, no schema/billing. Marketing review's #1 top-3, tagged data-engineer-safe | security-hardening / trust | marketing-reports/2026-07-06 (C1) | low | shipped 2026-07-06 (d307f31) |
| 9 | Reuse `components/EmailAlertForm.js` on the homepage (`app/page.js`) + the SEO landing pages (`app/sizes/[size]`, `app/tires/[brand]`, intent-matched query) — today it only appears on a zero-result browse search. Launch-phase demand capture + A2 privacy/incentive copy | launch-readiness / acquisition | marketing-reports/2026-07-06 (A1/A2) | low | shipped 2026-07-08 (0c723ea) |
| 10 | Link `app/sizes/[size]` to `/guide` + FAQ block for parity with `app/tires/[brand]` (dwell/SEO/trust) | acquisition / SEO | marketing-reports/2026-07-06 (A3) | none | shipped 2026-07-06 (2f8b06f) |
| 11 | C2 — used-tire TRUST WEDGE. Lightweight version SHIPPED (5a28edb): "Before you pay" inspect checklist (reusing BUYER_CHECKLIST) on listing detail. **Remaining for Evan (business/liability decision, not code):** a real returns/guarantee/buyer-protection mechanism (who bears risk, inspection window, disputes, possible escrow) — the engineer must not invent the policy | trust / conversion | marketing-reports/2026-07-06 (C2) | low (lightweight) / L (guarantee) | lightweight shipped 2026-07-08 (5a28edb); guarantee mechanism = Evan |
| 12 | BRAND SOURCE-OF-TRUTH — RESOLVED. Evan chose "restore true brutalism". Rendering the app showed it was ALREADY brutalist (globals.css `border-radius:0 !important` overrides the rounded-* classes; .card/.btn are hard-offset square). The review's "dark-SaaS" read was an un-rendered class-name inference. Completed the conversion: flattened 12 gradients + hardened shadow tokens (5bac7c6). README's "brutalist canonical" was correct | brand / design | marketing-reports/2026-07-06 | low | shipped 2026-07-09 (5bac7c6) |
| 14 | FIX (pre-existing, low): `/pro` (app/pro/page.js:30) + `/sell/bulk` (app/sell/bulk/page.js:17) feature panels put `text-black` copy on a dark `bg-brand-500/15` fill (~1.5:1, illegible). Not caused by the brutalist change (was identically illegible before) — flagged by the 2026-07-09 design review. Fixed: panel wrapper text-black->text-white (verified by authed render) | accessibility / bug | design review 2026-07-09 | low | shipped 2026-07-09 (b1e2406) |
| 13 | HYGIENE (optional): remove the ~112 dead `rounded-*` classes + the 2 unused `borderRadius` config values — they're overridden by globals.css `border-radius:0 !important` (zero render change) but their presence is exactly what made the marketing reviewer misread the app as "dark-SaaS". Done: 121 dead tokens removed across 39 files (Stars.js var + globals comment preserved), zero render change verified by render. Kept the !important guardrail | code-hygiene | 2026-07-09 brand investigation | none (no render change) | shipped 2026-07-09 (5e8f4d0) |

| 16 | PROPOSAL: drop enumeration hint from `/api/email-alerts` (existing `(email,query)` pair returned `{ok:true,duplicate:true}`, distinguishable from the fresh-create `{ok:true}` response) — now neutral, matching BACKLOG #1's enumeration-safe symmetry. Recovers the substance of a proposal from closed stale PR #13 (bit-rotted), re-implemented fresh. | security-hardening | recovered from closed PR #13 | low (auth-adjacent → PROPOSAL) | shipped-to-branch 2026-08-24 (468c96f) |
| 17 | `components/AuthForm.js` unconditionally showed the real, working demo login credentials (demo@tiretrader.test / demo1234, seeded by `prisma/seed.js:116`) to every anonymous `/login` visitor — a live account anyone could sign into. Gated on `NODE_ENV !== "production"` (same pattern `lib/auth.js` uses); still visible in local dev, gone from the real prod build. Verified live: curl of a `next start` build shows 0 occurrences of the credential string. | security-hardening / trust | marketing-reports/2026-08-24.md (top finding) | low (auth-adjacent → PROPOSAL) | shipped-to-branch 2026-08-24 (5322755) |

| 18 | PROPOSAL (shipped-to-branch): password-reset link built from `X-Forwarded-Host`/`Host` (client-controlled) instead of `SITE_URL` — host-header poisoning lets an attacker email a victim a genuine, passing-SPF/DKIM TireTrader reset mail whose link points at an attacker server, with a valid 1-hour token. Verified live pre/post with curl (evil.example → shoptiretrader.com). Last header-derived email link in the app; all others already used `SITE_URL`. | security-hardening | full audit 2026-08-31 | low (auth-flow → PROPOSAL) | proposal 2026-08-31 (2cb7326) |
| 19 | PROPOSAL (shipped-to-branch): `prisma/seed.js`'s destructive delete-everything-then-seed had no production guard, while DEPLOY.md:53/GO-LIVE.md:65 list `prisma db seed` as an optional prod step — one shell with a prod `DATABASE_URL` wipes the live marketplace and repopulates it with publicly-documented credentials. Added `assertLocalDatabase()` (file:/localhost only; `ALLOW_REMOTE_SEED=1` escape hatch; creds redacted in refusal output). | security-hardening / data-integrity | full audit 2026-08-31 | low (destructive-op guard → PROPOSAL) | proposal 2026-08-31 (aa5881e) |
| 20 | PROPOSAL (shipped-to-branch): the 2026-08-27 demo-admin remediation (9b4b8c4/4272ed8) covered only `demo@tiretrader.test`; `prisma/seed.js` creates 4 more accounts (`buyer@`/`mike@`/`rosa@`/`ken@`) with equally-public passwords (`README.md:93`, and the full derivation scheme is published in plaintext JS at `docs/index.html:264`) — mike is Pro, rosa is a founding seller, all carry seeded reviews. `scripts/neutralize-demo-admin.cjs` generalized from one hardcoded account to all 5 seeded accounts, idempotent, dry-run verified (`already=1 would-fix=4`). **ACTION FOR EVAN: still needs to run against the prod Neon URL** — the 4 accounts remain live until then; command in the PR body / safety report. | security-hardening | full audit 2026-08-31 (re-verification of 8/27 fix scope) | low (tool, not a route change → PROPOSAL) | proposal 2026-08-31 (c460810) — **prod run pending, Evan** |
| 21 | PROPOSAL (shipped-to-branch): per-account rate limits were bypassable by IP rotation — 11 call sites passed the user id inside the `action` string instead of `opts.key`, so `lib/security.js`'s bucket key kept appending the IP (`upload:<uid>:<ip>`) rather than replacing it, giving a full fresh quota per source address. A/B proven on real builds (pre-fix: 8 req from 8 IPs → all 200; post-fix: 429 on the 6th). | security-hardening | full audit 2026-08-31 | medium (broad but mechanical key-arg fix → PROPOSAL, 9 files) | proposal 2026-08-31 (af511a3) |
| 22 | PROPOSAL (shipped-to-branch): EXIF/GPS stripping only covered JPEG uploads — PNG/WebP kept embedded GPS. `lib/image.js` now strips all three formats; round-tripped real GPS-tagged test images to confirm data removed and images still decode. Suite 66→74. | security-hardening / privacy | full audit 2026-08-31 | low (localized image-pipeline fix → PROPOSAL) | proposal 2026-08-31 (cfc7002) |
| 23 | FLAGGED (not fixed — monetization, needs Evan): `app/api/stripe/checkout/route.js:19-29`'s dev-simulation branch is gated only on `stripeConfigured()`, with no `NODE_ENV`/deployment guard (unlike the sibling `/api/pro` route). If `STRIPE_SECRET_KEY`/`STRIPE_PRICE_ID` are unset or still `REPLACE_ME` in production, any logged-in user gets a free 30-day "active" subscription on one POST, which then also unlocks free Pro. Recommended fix: fail closed (500) when deployed, mirroring `lib/auth.js:14-20`'s `IS_DEPLOYED` pattern — but this is billing/monetization control flow, outside this routine's remit even as a proposal. **Check the Vercel Stripe env values today** — local `.env` still has `price_REPLACE_ME`. | monetization / security | full audit 2026-08-31 | n/a (flag-only, billing) | flagged 2026-08-31 — needs Evan |
| 24 | FLAGGED (low/latent): `app/sellers/[id]/page.js:19` Prisma `include` (no `select`) pulls another user's `passwordHash`/`email`/`resetTokenHash`/`stripeCustomerId` into the server render. Not leaked today (only `seller.id` reaches the client component), but one refactor away from shipping password hashes to every visitor — add an explicit `select`. | security-hardening | full audit 2026-08-31 | low | flagged 2026-08-31 |
| 25 | FLAGGED (low): `data:` base64 photo URLs (`app/api/listings/route.js:66`, `[id]/route.js:80`) bypass the upload pipeline's EXIF/GPS stripping entirely, stored verbatim. | privacy / security-hardening | full audit 2026-08-31 | low | flagged 2026-08-31 |
| 26 | FLAGGED (low): `app/api/client-error/route.js:30-34` spreads unauthenticated request JSON wholesale into the outbound Slack webhook payload — an attacker can inject fields (`blocks`, `channel`). Pick fields explicitly instead of spreading. | security-hardening | full audit 2026-08-31 | low | flagged 2026-08-31 |
| 27 | FLAGGED (informational, re-confirmed): `sharp`/libvips CVEs (GHSA-f88m-g3jw-g9cj, 2 high) — clearing needs sharp→0.35.x, a minor transitive bump (tested: `next@15.5.24` alone does not resolve it, reverted). Practical exposure assessed low: zero `next/image` usage, no `images.remotePatterns`, `/_next/image` rejects remote URLs (400). Fix when ready: `npm audit fix --force`. | security-hardening | npm audit, re-confirmed 2026-08-31 | none (dependency minor bump, flag-only) | flagged — carried since 2026-07-06 |

Refresh sources each run: latest flagged findings, newest `marketing-reports/*.md` recommendations,
`improve:` lines in ~/.claude/agent-logs/dev.log, and fresh inspection.
